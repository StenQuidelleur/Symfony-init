{% extends 'base.html.twig' %}

{% block content %}
    <div class="card col-6" >
        <div class="card-body">
                <h1 class=" card-title">Saison{{ season.number }} de {{ program.title }} ({{ season.year }})</h1>
                <p class="card-text">{{ season.description }}</p>
                <p class="card-text">Liste des episodes: </p>
                <ul class="list-group">
                    <li class="list-group-item">{{ episode.number }} : {{ episode.title }}</li>
                </ul>
        </div>
    </div>

    <a href="{{ path('wild_index') }}" class="btn btn-success">
        Retour à l'accueil
    </a>
{% endblock %}