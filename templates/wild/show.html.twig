{% extends 'base.html.twig' %}

{% block content %}
    <!--<h1>{#{{ slug }}#}</h1> === Quête 05 Symfony === -->
    <div class="card media col-4" >
        <img class=" card-img-top align-self-start mr-3" style="width: auto; height: 300px;" src="{{program.poster}}" alt="{{ program.title }} poster">
        <div class="card-body media-body">
            <h1 class=" card-title mt-0">{{ program.title }}</h1>
            <p class="card-text">{{ program.summary }}</p>
            <p class="card-text">Categorie : {{ program.category.name }}</p>
            <a href="{{ path('wild_season', { 'id': 1 }) }}" class="btn btn-primary">Saisons</a>
        </div>
    </div>

    <a href="{{ path('wild_index') }}" class="btn btn-success">
        Retour à l'accueil
    </a>
{% endblock %}